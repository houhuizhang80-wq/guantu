# 《官途》开发报告

> 文档日期：2026-09-13（含当日全部改动）
> 工程路径：`D:\2026\ai\xiaomi\guantu`
> 仓库：<https://github.com/houhuizhang80-wq/guantu>（公开，main @ `0030aac`）
> 在线试玩（GitHub Pages）：<https://houhuizhang80-wq.github.io/guantu/>
> 玩家交流 QQ 群：`1107570877`
> 交付物：`官途-可直接打开.html` / `docs/index.html`（Pages 入口）
> 当前版本：构建产物 **638,396 字节 / sha256 `81F5EEE9ECEC9D03`**（dist、根目录单文件、docs、publish 四份一致）
> 源码规模：src 75 个 TS 文件 / 约 26.5k 行 + `style.css` 约 2.8k 行，scripts 15 个

---

## 1. 项目定位

架空官场生涯模拟（文本决策）。从乡镇办事员起步，历经真实感选拔程序与年龄约束，目标可至中央最高层（总理 / 委员长 / 政协主席 / 国家主席 / 军委主席 / 纪委书记等）。

**成功标准：**

1. 职级称呼符合中国国情
2. 任职年龄与现实节奏接近（登顶多在 58–65 岁）
3. 晋升走推荐→考察→公示→票决→任免
4. 防乱点、防空转
5. 16 出身 × 30 省差异化
6. 纯属虚构，协议与 UI 持续声明

---

## 2. 技术栈

| 项 | 说明 |
|----|------|
| 框架 | Vite 8 + TypeScript，手写 DOM（无 UI 框架） |
| 样式 | `src/style.css`，党政红主题，支持浅色 / 深色 |
| 音效 | WebAudio 合成 |
| 账号 | 云端 Auth（邮箱 + 密码 / 邮箱验证码）；GitHub Pages / 本地单文件为离线模式 |
| 存档 | localStorage 本机镜像 + 可选云端同步；导出 `.guantu` 为 **AES-GCM 加密** |
| 依赖 | `@tencent-ai/workbuddy-cloud-sdk@dev`（云能力 SDK；玩家可见文案已去品牌化） |
| 交付 | `vite-plugin-singlefile`（单文件自包含） |
| 版本 | Git → GitHub（origin 使用 SSH：`git@github.com:houhuizhang80-wq/guantu.git`） |
| 测试 | smoke / sim 脚本 + `scripts/verify_export.ts`（加密往返） |

**命令：**

```bash
npm install
npm run dev
npm run build          # 产出 dist/index.html，需覆盖 官途-可直接打开.html / docs/index.html / publish/index.html
npm run smoke
npm run smoke:duty
npm run smoke:benxiang
npm run smoke:extra
npm run sim
npm run sim:smart
npm run sim:summit
npx tsx scripts/verify_export.ts                 # AES-GCM 导出导入往返自检
npm exec -- tsx scripts/verify_endings.ts   # 结局判定回归（壮年期误收档等）
```

---

## 3. 源码结构

```text
src/
├── main.ts              # 主循环与交互（advanceMonth / resolveChoice / handlers）
├── types.ts             # 全局类型
├── style.css            # 党政红 UI（含深色主题变量）
├── data/                # 40 个静态数据文件
├── systems/             # 21 个玩法系统
├── state/               # 8 个：存档 / 开局 / 账号 / 云客户端 / 跨局进度 / 同步编排
└── ui/                  # 4 个：渲染 / 音效 / 分享 / 水印
scripts/                 # smoke（4）与 sim（5）、结局回归、导出加密自检等
docs/index.html          # GitHub Pages 入口（构建产物副本）
```

**`systems/`（21 个）**

| 文件 | 职责 |
|------|------|
| `promotion.ts` | 晋升五步选拔、票决、调参锚点 `TUNE` |
| `jijian.ts` | 纪检监察流程（线索→函询→初核→立案→审理→移送检察） |
| `goals.ts` | **年度目标责任书**（1 月签订 / 12 月结算） |
| `secretary_case.ts` | **秘书腐败分支**（触发 / 三条处置线 / 把柄反噬） |
| `extra.ts` | 督查暗访、谈心谈话、项目攻坚与主官专项、子女升学就业、回忆录 |
| `office.ts` | 舆情、周计划、调研、秘书、台账、名册推荐 |
| `duty.ts` | 深度公务（批示 / 信访 / 起草 / 会议角力 / 培训） |
| `faction.ts` | 派系角力、会议表决、派系声望 |
| `network.ts` | 关系网、羁绊、靠山、写信 |
| `appraisal.ts` | 年度考核与述职侧重 |
| `events.ts` | 事件筛选与加权抽取（`scheduleNextEvent`） |
| `engagement.ts` | 防乱点（草率分、经手事件门槛、快进荒政） |
| `age.ts` `retired.ts` | 年龄线、退休余热 |
| `risk.ts` `floats.ts` `focus.ts` `favors.ts` `family.ts` `score.ts` `ending.ts` | 风险漂移、飘字、月度焦点、人情、家属、评分、结局调度 |

**`data/` 要点**

| 文件 | 内容 |
|------|------|
| `posts.ts` `posts_extra.ts` | 89 个岗位节点与晋升路径 |
| `events*.ts`（20 个） | 事件池，含 `events_meeting.ts`（议事与会议）与 `events_central.ts`（中央 + 部委条线） |
| `endings.ts` | 28 个结局与收档判定 |
| `changelog.ts` | **游戏内更新日志**（倒序，kind：新增 / 修复 / 优化） |
| `help.ts` | 帮助文档二十节 |
| `banned_words.ts` `reserved_names.ts` | 昵称合规（纯函数 + 名单） |
| `origins.ts` `provinces.ts` `achievements.ts` `npcs.ts` `duties.ts` `projects.ts` `promo_strategies.ts` | 出身 / 省份 / 成就 / NPC / 公务 / 台账 / 策略 |

**`state/` 各文件职责**

| 文件 | 职责 |
|------|------|
| `cloud.ts` | 云客户端（懒初始化）、发布域名判定、错误文案归一 |
| `auth.ts` | 邮箱注册 / 登录 / 验证码 / 改密、账号档案读写、会话恢复（8 秒超时） |
| `saves.ts` | 3 个槽位：本机镜像 + 云端推送 / 拉取 / 双向合并；旧档字段补齐 |
| `progress.ts` | 跨局累计（图鉴 / 成就 / 出身通关）并集同步 |
| `cloudsync.ts` | 登录后同步编排、本机数据归属与账号切换清理 |
| `game.ts` | 开局与存档门面 |
| `catalog.ts` `origins_done.ts` | 本机跨局记录 |

---

## 4. 内容规模（2026-09-13 实测）

| 维度 | 数量 |
|------|------|
| 岗位节点 | 89（posts 61 + posts_extra 28） |
| 事件 | 355（主线 77 / 日常 193 / 人脉 35 / 危机 30 / 平顺 20） |
| 事件选项 | 1075 |
| 议事场景 | 15 条事件，9 类会议（县常委会 / 政府常务会 / 书记专题会 / 民主生活会 / 市委常委会 / 党政联席会 / 市委全会 / 省委常委会 / 党组会） |
| 主官专项 | 3 条（招大引强 / 债务化解 / 生态督察整改） |
| 秘书腐败分支 | 1 套（触发门槛 / 三条处置线 / 把柄反噬） |
| 反贪条线 | 2 条事件 + 纪检移送衔接 |
| 出身 | 16 + 隐藏「老档案重生」 |
| 省份 | 30 架空省 |
| 结局 | 28（含正国分岗专属） |
| 成就 | 27 |
| 深度公务 | 批示 / 信访 / 起草 / 会议角力 / 培训 |
| 经营系统 | 舆情 / 周计划 / 调研 / 秘书 / 督查 / 攻坚 / 回忆录 |
| 跨阶段羁绊 + 靠山 | 写信、铺路 |
| 多周目 | 余荫 + 门生继承 |

---

## 5. 核心机制

- **五维**：政绩 / 关系 / 廉洁 / 民心 / 能力 + 风险
- **晋升五步** + 试用期 + 届中更难
- **年龄**：开局 23–34；到龄退休（副处以下 60 / 县处 63 / 厅局 65 / 省部 68 / 国家级 70）
- **纪检**：线索→函询→初核→立案→审理；严重程度 ≥75 时追加「涉嫌犯罪问题移送检察机关」
- **年度目标责任书**：每年 1 月按职务层次签订 2–3 项指标（基层考执行力、中层考守线、高层考结果），12 月对照结算——全部达成政绩 / 民心 / 能力齐涨且降风险，全部落空抬风险并伤民心；奖惩在年度考核**之前**落账，因此会直接反映到考核分
- **主官专项**：任地方党委 / 政府正职（镇到省）后开放三条专项，在办有月度压力（债务线每月风险 +1），收官按质量与廉洁差异化结算
- **议事与会议**：县委常委会、政府常务会、书记专题会、民主生活会、市委常委会、党政联席会、市委全会、省委常委会、党组会（三重一大）随职务层次出现，表态影响政绩、声望、派系与风险
- **身边人出事**：联络员熟练度 ≥60/≥75 后按概率被举报，可切割 / 保全 / 报告调离；**保全留下的把柄会在纪检立案时把严重程度 +10**
- **反贪条线**：检察院调卷、约谈随机出现，打招呼摆平的失败代价高于如实配合
- **防乱点**：草率分、经手事件门槛、快进荒政
- **登顶难度**（稳妥 Bot，1152 局 / 12 种子）：正国 11.5% / 副国 16.2% / 省部正 20.7%（详见第 7 节）

---

## 6. 已修主要问题（累计）

**玩法与数据**

- 晋升策略失败仍白嫖五维；高层 GX 9 折软放行是死代码；选拔中途立案仍可上任
- 出身 Risk 负数开局 / 本乡锁死 / 乱用选调模板；省级地名不本地化 / 沿海 Risk 不生效
- 关系网永远盯 NPC[0] / 高层新人不入场
- 派系倾覆过早 / 本乡好名声过早截断；`paixi_fu` 翻车率过高（52%→37%）
- 收档类结局缺「仍有去路」保护，7 个结局会误收档
- **壮年期被误判终局**（2026-09-13）：36 岁副处因三年未晋升被判「家和事兴」；正科 40 岁前后被判「生涯暂缓」。已加壮年期保护（age<50 且岗位有上一级去路不收档）+ 补年龄门槛

**体验与工程**

- 深色主题对比度：事件正文等硬编码深色文字改随主题；约 15 处硬编码浅色底变量化
- 对局内点选项 / 切页签丢失滚动位置、周计划未提交选择被清空
- 弹层不支持 Esc 关闭、无焦点圈定（已统一 `wireModal`，强制决策弹层刻意除外）
- 页签按钮与容器共用 `data-tab` 属性导致点击判定歧义（按钮已改 `data-nav`）
- 标题页「设置 / 制度说明」弹层从未挂载 → 点击无响应（已提升到 `renderApp` 统一挂载）
- 冷启动偶发停在启动页（登录态恢复加 8 秒上限）
- 站点无图标导致控制台 404（已加内联空图标）

---

## 7. 平衡锚点（当前）

调参集中在 `src/systems/promotion.ts` 的 `TUNE` 对象。

| 项 | 参考值 |
|----|--------|
| 票决通过线 | 正国 73 / 进副国 80 / 正部级 78 / 省部副 76 / 其余 48 |
| 进省部正压制 | -26（廉洁<86 或政绩<98 再 -16） |
| 进省部副压制 | -20（廉洁<80 或政绩<96 再 -15） |
| 进副国压制 | -13（廉洁<88 / 能力<90 / 民心<68 再 -11） |
| 副国→正国压制 | 基础 0（廉洁<78 / 能力<82 / 民心<62 再 -3） |
| 国家级风险线 | 进副国 risk>8 起罚（×1.0）；副国→正国 risk>14 起罚（×0.25） |
| 经手事件门槛 | 副国 14 / 正部 12 / 厅局 8 / 县处 6 / 乡科 5 / 其余 3 |
| 草率分上限 | 副国 16 / 正部 25 / 厅局 40 / 其余 55 |
| 副国最低任期 | 24–36 月（按岗位），新任试用期 12 个月 |
| 关系软放行 | 仅 rank<15 生效：门槛 ≥70 时按 92% 放行 |

### 难度校准（稳妥 Bot，1152 局 / 12 种子）

| 最高职级 | 实测 | 目标 | 偏差 |
|----------|------|------|------|
| 国家级正职 | 11.5% | 10% | +1.5pt |
| 国家级副职 | 16.2% | 15% | +1.2pt |
| 省部级正职 | 20.7% | 20% | +0.7pt |

调参方法见 `scripts/sweep_difficulty.ts`（固定随机种子跑多组 `TUNE`，配对对比压低方差）。

**有效杠杆只有四个**：`buBase` / `lineBu` / `guoBase` / `lineTop`。其余参数（`guoWeak`、`topWeak`、`topBase` 等）在实测区间内逐局无差异。**断崖**：`lineTop` 72→78、`lineGuo` 80→85 会让登顶率从约 12% 直接跌到约 6%，中间无过渡。

**调参铁律**：只用固定随机种子做配对扫描，每配置 ≥768 局；随机种子的 48 局结果噪声远大于信号。三档难度强耦合，收紧门槛会推高正国率、压垮省部正档，不能单点调。

---

## 8. 存档、导出与账号

### 存档槽位

- **6 个槽位**（`SLOT_COUNT = 6`，localStorage 键 `guantu_slot_0…5` / `guantu_slot_at_*` 动态生成）
- 标题页「最近存档」、跨局成就合并、云同步均按 `SLOT_COUNT` 扫描
- 云端 `saves` 表仍按 `(owner_id, slot)` 唯一；客户端写入前 clamp 到 `[0, 5]`

### 导出 / 导入（AES-GCM，非明文）

| 项 | 说明 |
|----|------|
| 新格式 | `GUANTU2:` + Base64(salt16 ‖ iv12 ‖ ciphertext) |
| 算法 | PBKDF2-SHA256（180,000 次）→ AES-256-GCM |
| 密码 | 导出必填 ≥4 位并二次确认；导入 `GUANTU2` 时必填 |
| 整包内容 | `{ save: GameState, progress: { catalog, originsDone, promoFails } }` |
| 兼容导入 | 旧 `GUANTU1:`（XOR+Base64）与旧明文 JSON |
| UI | 设置 →「导出加密存档」/「导入备份」；密码弹层 `promptSavePassword` |
| 自检 | `scripts/verify_export.ts`：往返正确、错误密码拒绝、无明文字段泄露 |

### 云端账号（可选，域名绑定）

账号依赖服务端按 Origin 精确匹配的正式域名。**GitHub Pages / 本地单文件 / localhost / file:// 一律离线模式**：无登录入口，存档只写本机浏览器。玩家可见文案已统一为「云端服务」，不再展示失效的旧云域名。

若仍维护云端部署（应用 id `wbapp_tpjoiEKbuRfQjCdrw6bSu9`）：

| 表 | 用途 | 关键约束 |
|----|------|----------|
| `profiles` | 账号档案：昵称、注册时间 | `owner_id` 唯一 |
| `saves` | 存档槽位，整份 GameState 存 JSONB | `(owner_id, slot)` 唯一 |
| `progress` | 跨局累计：图鉴 / 成就合集 / 出身通关 | `owner_id` 唯一 |

- 三张表全部开启 RLS，策略为 `owner_id = auth.uid()` 的 owner-only。
- 客户端**不发送** `owner_id`，由服务端 `DEFAULT auth.uid()` 填写。

### 昵称合规

昵称在注册页、账号中心、写入 `profiles` 前各校验一道，规则在 `src/data/banned_words.ts` / `reserved_names.ts`：结构规则 + 词表 + 机构职务形态正则 + 公众人物名单。

### 离线运行与玩家入口

- 离线页主文案：**离线可玩**；meta 首行 **玩家交流 / QQ 群 1107570877**；不再展示 WorkBuddy / 失效正式入口
- 标题页、设置、帮助、用户协议、登录页页脚均可打开 QQ 群弹窗
- 离线仍可 6 槽位自动存档 + 导出加密备份

---

## 9. 交付与发布

- **单文件交付**：`官途-可直接打开.html`，自包含，双击即玩
- **公开试玩**：GitHub Pages — <https://houhuizhang80-wq.github.io/guantu/>（`docs/index.html`）
- **仓库**：<https://github.com/houhuizhang80-wq/guantu>
- **发布流程**：

```bash
npm run build
cp dist/index.html 官途-可直接打开.html
cp dist/index.html docs/index.html
cp dist/index.html publish/index.html
git add -A && git commit -m "..." && git push origin main
# Pages 约数分钟内自动更新
```

> 本机访问 GitHub 时 HTTPS 可能被重置；origin 已改为 SSH（`git@github.com:houhuizhang80-wq/guantu.git`）。若 push 失败，先 `ssh -T git@github.com` 验证密钥。

**校验**

1. 构建后核对四份产物 sha256 一致
2. Pages 刷新后检查离线页是否出现「QQ 群 1107570877」且无旧域名
3. `npx tsx scripts/verify_export.ts` 验证加密往返

---

## 10. 更新日志（游戏内）

游戏内「更新日志」数据与源码同源：`src/data/changelog.ts`（倒序，最新在最上，kind 为新增 / 修复 / 优化）。

**三处入口**（缺一就会出现玩家找不到）：

1. 云端登录页页脚〔更新日志〕〔玩家交流群〕
2. 对局中：底部工具 → 设置 → 「更新日志（版本号）」；设置内另有〔玩家交流群〕
3. 离线 / 单文件 / Pages：标题页 → 设置 或 离线页〔更新日志〕〔玩家交流群〕

> 维护约定：**每次上线新内容，都要往 `CHANGELOG` 顶部补一条**，否则玩家看不到这次改了什么。

**版本历史**

| 版本 | 主要内容 |
|------|----------|
| 2026.09.13c | 标题页 / 设置 / 离线页可打开 QQ 群 1107570877；离线页去掉失效旧云入口；文案去 WorkBuddy 品牌 |
| 2026.09.13b | 存档槽位 3→6；导出改为 AES-GCM（GUANTU2）加密包，导入需密码；加密包含图鉴 / 出身通关 |
| 2026.09.13 | 修复壮年期误判终局；修复冷启动长时等待；新增游戏内更新日志；登录页玩家交流群；消除图标 404 |
| 2026.09.12 | 年度目标责任书；地方主官专项三条；议事与会议 15 处；身边人出事分支；检察反贪条线；中央部委条线事件 10 条；深色主题与滚动 / 草稿 / 弹层体验修复；字号三档、帮助目录、行动点与同步状态 |

---

## 11. 已知问题与待办

| 项 | 状态 |
|----|------|
| `jixu`（turn ≥ 560）与到龄退休 `tuixiu`（优先级 95）冲突，该结局近似不可达 | 待处理 |
| 带 `textByOrigin` 出身专属正文的事件仅 2 条 | 待扩充 |
| 云端账号链路依赖域名绑定；GitHub Pages 为离线模式，无法在该域名验证云登录 | 按需另开正式域名 |
| 原 WorkBuddy 线上地址曾返回 404「链接已失效」；公开试玩以 GitHub Pages 为准 | 已切换 |
| 本机 push GitHub HTTPS 偶发被重置，已改 SSH | 已规避 |

---

## 12. 免责

架空虚构，不影射现实人物与行政区划；制度设定为玩法化压缩，不构成政策解读。
